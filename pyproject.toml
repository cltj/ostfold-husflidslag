[project]
name = "ostfold-husflidslag"
version = "0.1.0"
description = "Dette prosjektet omhander østfolds husflidslag`s online aktiviteter"
readme = "README.md"
authors = [
    { name = "thor kinstad", email = "tj@clodlink.ai" }
]
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=1.17.0",
    "pytest>=8.4.0",
    "ruff>=0.12.0",
    "uv>=0.8.0",
]


[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
